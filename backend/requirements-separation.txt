# Optional real speech separation backend.
# Install PyTorch CPU wheels first on Windows:
# backend\.venv\Scripts\python.exe -m pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
speechbrain
soundfile
